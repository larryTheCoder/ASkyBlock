checkout:
  post:
    - git submodule sync
    - git submodule update --init
    - mvn clean package
    - cp -R ./target/SkyBlock-0.3.9-STABLE.jar $CIRCLE_ARTIFACTS

machine:
  java:
version: oraclejdk8
