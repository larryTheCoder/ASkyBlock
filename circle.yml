checkout:
  post:
    - git submodule sync
    - git submodule update --init
    - mvn clean package
    - cp -R ./target/SkyBlock-${project.version}.jar $CIRCLE_ARTIFACTS

machine:
  java:
version: oraclejdk8
